{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @11ty/eleventy --input=./src --output=dist",
    "serve": "npx @11ty/eleventy --input=./src --output=dist --serve --watch",
    "dev": "npm run serve",
    "publish": "node publish.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Berenddeperend/blog.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Berenddeperend/blog/issues"
  },
  "homepage": "https://github.com/Berenddeperend/blog#readme",
  "dependencies": {
    "@11ty/eleventy-img": "^2.0.1",
    "gh-pages": "^3.2.0",
    "markdown-it": "^12.0.4",
    "sharp": "^0.30.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "gh-pages": "^3.2.0"
  }
}

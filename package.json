{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @11ty/eleventy --input=./src --output=dist",
    "serve": "npx @11ty/eleventy --input=./src --output=dist --serve",
    "docker:build": "docker build -t berenddeperend/blog .",
    "docker:push": "docker push berenddeperend/blog"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Berenddeperend/blog.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Berenddeperend/blog/issues"
  },
  "homepage": "https://github.com/Berenddeperend/blog#readme",
  "dependencies": {
    "markdown-it": "^12.0.4"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1"
  }
}
